# Optional. Raises the GitHub API rate limit for project data (60/h anonymous -> 5000/h).
GITHUB_TOKEN=
# Public site origin used in canonical/OpenGraph URLs, sitemap.xml and robots.txt (Vite exposes VITE_* vars to both server and client).
VITE_SITE_URL=https://guille-gallo.github.io
