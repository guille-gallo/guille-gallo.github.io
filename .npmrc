# Enforce pnpm as the only package manager for this project.
# `engine-strict` makes pnpm honor the engines field; `only-allow` (via preinstall)
# blocks npm/yarn. Keeping this file also stops npm from writing package-lock.json.
engine-strict=true
